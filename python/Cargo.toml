[package]
name = "closest_core"
version = "0.2.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "closest"
crate-type = ["cdylib"]

[dependencies]
pyo3 = "0.23.5"
closest = { version = "0.2.0", path = "../" }
